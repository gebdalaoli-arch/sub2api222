export component AboutScreen inherits Rectangle {
    height: 124px;
    background: #eef3dc;
    border-radius: 18px;

    Text {
        x: 22px;
        y: 18px;
        text: "V1 边界";
        color: #233023;
        font-size: 18px;
        font-weight: 700;
    }

    Text {
        x: 22px;
        y: 50px;
        width: parent.width - 44px;
        text: "只做 CDK 兑换与公告式充值说明；不展示连接密钥、不展示服务地址、不接管官方账号。";
        color: #516047;
        font-size: 14px;
        wrap: word-wrap;
    }
}
